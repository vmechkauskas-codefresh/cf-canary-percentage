
'use strict';

angular
    /**
     * root controller
     */
    .module('cf-cp.layout')

    .controller('layoutController', function ( $scope ) {

        var root = $scope.root = {};
        // no-unused-vars =)
        root;

    });
